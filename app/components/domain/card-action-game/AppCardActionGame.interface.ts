import type { IllustrationName } from "~/components/ui/illustration/illustrations"

export interface AppCardActionGameClassProps {
    title?: string
    subtitle?: string
    buttonText?: string
    to?: string
    target?: string
    illustration?: IllustrationName
}